"""NanVix PIL — lightweight pure-Python image library for python-pptx.

Provides real image header parsing (PNG, JPEG, GIF, BMP, TIFF) for
dimension detection without requiring the Pillow _imaging C extension.
"""
